# מסמך אפיון מערכת (PRD)
## מיפל — גן אירועים בעיר (Maple Garden Events)

**גרסה:** 1.1  
**תאריך:** 8 ביולי 2026  
**סטטוס:** מעודכן — החלטות תפעוליות ואישור דרישות (RBAC, EasyCount, WhatsApp, AWS)

---

## פרטי פרויקט (מסגרת)

| שדה | תוכן |
|-----|------|
| **תיאור הפרויקט** | מערכת ניהול מקיפה לאולם אירועים כשר בישראל, המאפשרת ניהול לוח שנה, אופציות, הזמנות, חוזים, תשלומים, הפקת אירועים, קבלה LIVE, משוב לקוחות ותקשורת אוטומטית — הכל מממשק אדמין אחד בעברית ו-RTL. |
| **קהל יעד** | צוות האולם (מנהלים, מזכירות, הפקה), לקוחות קצה (דף משוב ציבורי בלבד) |
| **טכנולוגיות ליבה** | React 19 + TypeScript + Vite, Node.js + Express 5, PostgreSQL (AWS RDS) + Prisma, Socket.io, Google OAuth + RBAC, TanStack Query, AWS ECS/App Runner + S3 (SSE), EasyCount (חשבוניות), Puppeteer (PDF), Tesseract (OCR צ'ק), Nodemailer, WhatsApp API (Green API / Twilio), Sentry, Playwright |

---

# 1. סיכום מנהלים (Executive Summary)

## 1.1 מטרת הפרויקט

לבנות **מערכת ניהול אירועים מרכזית** עבור "מיפל — גן אירועים בעיר", שתחליף תהליכים ידניים (Excel, WhatsApp, ניירת) בזרימת עבודה דיגיטלית אחודה: מרגע שמירת אופציה בלוח השנה, דרך סגירת הזמנה וחתימה דיגיטלית, ועד הפקת האירוע, קבלת לקוח בזמן אמת, גביית תשלומים ומשוב לאחר האירוע.

## 1.2 הבעיה שהמערכת פותרת

אולמות אירועים בישראל מתמודדים עם:

- **כפילויות וחפיפות** — הזמנת אותו תאריך/משבצת פעמיים
- **פיזור מידע** — פרטי לקוח, תפריט, תשלומים וחוזה במקומות שונים
- **עומס תפעולי** — מעקב אחר אופציות שפגו, צ'קי ביטחון, טפסי הפקה ותזכורות ידניות
- **חוסר שקיפות** — קשה לדעת מה קורה "עכשיו" באירוע LIVE
- **משוב לא מסודר** — אין מנגנון מובנה לשליחת סקרים וניתוח שביעות רצון

## 1.3 ערך עסקי צפוי

| יעד | מדד הצלחה (KPI) |
|-----|-----------------|
| מניעת כפילויות | 0 אירועים כפולים באותה משבצת (אילוץ DB ייחודי) |
| קיצור זמן סגירת הזמנה | מ-< 2 שעות ידני ל-< 30 דקות דיגיטלי |
| שיפור גבייה | ירידה בצ'קי ביטחון חסרים (התראות אוטומטיות) |
| שביעות רצון לקוחות | > 60% מילוי משוב תוך 7 ימים |
| יעילות צוות | גישה מיידית לכל מידע האירוע מלוח בקרה אחד |

## 1.4 היקף (Scope)

**בהיקף (In Scope):**

- לוח שנה דו-תאריכי (עברי + לועזי)
- ניהול אופציות והזמנות
- טפסי הפקה + תוכנית ישיבה
- קבלה LIVE באירוע
- הגדרות מתחם (תמחור, שירותים, כשרות, משתמשים)
- משוב לקוחות + סטטיסטיקות
- שליחת ברכות (מיידית / מתוזמנת)
- אינטגרציות: Google Auth, EasyCount (3 סוגי מסמכים), אימייל, WhatsApp API רשמי, S3
- RBAC — 4 תפקידים מוגדרים (Manager, Staff, Production, Floor Staff)
- מיגרציית נתונים היסטוריים מ-Excel (2026–2027)

**מחוץ להיקף (Out of Scope — שלב ראשון):**

- אפליקציית מובייל Native
- פורטל לקוח מלא (צפייה בחוזה / תשלום עצמאי)
- סליקת אשראי מקוונת (Cardcom / Tranzila) — תשלומים ידניים בלבד
- CRM חיצוני / ERP
- ניהול מלאי מטבח בזמן אמת

---

# 2. פרסונות משתמשים והרשאות (User Personas & Permissions)

## 2.1 פרסונות

### פרסונה 1: מנהל/ת האולם (Manager)

- **תיאור:** בעל/ת העסק או מנהל/ת תפעולי/ת עם גישה מלאה
- **מטרות:** פיקוח על לוח שנה, אישור הזמנות, מעקב כספי, ניהול הגדרות
- **כאבים:** חוסר תמונה מלאה, תזכורות ידניות ללקוחות
- **תדירות שימוש:** יומיומית

### פרסונה 2: מזכירות / נציג מכירות (Staff)

- **תיאור:** עובד/ת שסוגר אופציות והזמנות, מתקשר עם לקוחות
- **מטרות:** שמירת אופציה מהירה, שליחת הודעות, סגירת הזמנה
- **כאבים:** טעויות בהזנת נתונים, חיפוש הזמנות ישן
- **תדירות שימוש:** יומיומית

### פרסונה 3: מפיק/ת אירועים (Production)

- **תיאור:** אחראי/ת על טופס הפקה, תפריט, סידורי ישיבה, כשרות
- **מטרות:** השלמת טופס הפקה לפני האירוע, ייצוא תוכנית ישיבה
- **כאבים:** שינויים אחרונים שלא מתועדים
- **תדירות שימוש:** לפי אירוע

### פרסונה 4: צוות קבלה / אולם (Floor Staff)

- **תיאור:** מבצע קבלת לקוח בזמן האירוע (LIVE)
- **מטרות:** עדכון מנות בפועל, תוספות LIVE, חתימת לקוח
- **כאבים:** טפסים ניירתיים, חוסר סנכרון עם ההזמנה
- **תדירות שימוש:** ביום האירוע

### פרסונה 5: לקוח קצה (Customer — Guest)

- **תיאור:** זוג/משפחה שהשלימו אירוע
- **מטרות:** מילוי משוב קצר (אוכל / שירות / אולם)
- **גישה:** דף ציבורי בלבד (`/feedback/:token`), ללא התחברות
- **תדירות שימוש:** חד-פעמית

## 2.2 מודל הרשאות (RBAC)

המערכת מיישמת **בקרת גישה מבוססת תפקידים (Role-Based Access Control)** עם 4 תפקידים פנימיים ותפקיד חיצוני אחד. כל משתמש פנימי מאומת באמצעות Google OAuth ומשויך לרשומה בטבלת `AuthorizedUser` עם שדה `role` ייעודי.

| תפקיד | תיאור | אימות | הרשאות עיקריות |
|-------|--------|--------|-----------------|
| **Manager** (מנהל/ת) | בעל/ת העסק או מנהל/ת תפעולי/ת | Google OAuth + `AuthorizedUser` | גישה מלאה לכל המערכת: הגדרות, מחיקות, נתונים כספיים, ניהול משתמשים, הפעלת LIVE |
| **Staff** (מזכירות / מכירות) | עובד/ת קבלה ומכירות | Google OAuth + `AuthorizedUser` | צפייה בלוח שנה, יצירת אופציות, המרה להזמנות, שליחת הודעות. **ללא** הגדרות מערכת ו**ללא** מחיקות |
| **Production** (הפקה) | מפיק/ת אירועים | Google OAuth + `AuthorizedUser` | עריכת טפסי הפקה, תוכנית ישיבה, ניהול תפריט, גלריה. **ללא** גישה להזמנות, הגדרות או נתונים כספיים |
| **Floor Staff** (צוות קבלה) | עובד/ת אולם ביום האירוע | Google OAuth + `AuthorizedUser` | גישה **אך ורק** למסך קבלה LIVE ביום האירוע |
| **Customer** (לקוח קצה) | ממלא משוב לאחר אירוע | Token חד-פעמי (`/feedback/:token`) | שליחת משוב בלבד |
| **לא מורשה** | — | — | נחסם בכניסה |

**יישום טכני:**

- שדה `role` ב-JWT ובטבלת `AuthorizedUser` (ערכים: `manager`, `staff`, `production`, `floor_staff`)
- Middleware `requireRole(...)` בצד השרת לכל endpoint רגיש
- סינון פריטי תפריט ניווט (`navConfig`) ונתיבי Router בצד הלקוח לפי תפקיד
- רק **Manager** רשאי לנהל משתמשים מורשים ולהקצות תפקידים

## 2.3 מטריצת הרשאות לפי מודול

| מודול | Manager | Staff | Production | Floor Staff | Customer |
|-------|---------|-------|------------|-------------|----------|
| לוח בקרה | CRUD | R | — | — | — |
| לוח שנה | CRUD | R + Create Option | R | — | — |
| ניהול אופציות | CRUD | RU (ללא מחיקה) | — | — | — |
| ניהול הזמנות | CRUD | RU (ללא מחיקה) | — | — | — |
| טופס הפקה | CRUD | R | CRU | — | — |
| תוכנית ישיבה | CRUD | — | CRU | — | — |
| ניהול תפריט | CRUD | — | CRU | — | — |
| קבלה LIVE | CRUD + הפעלה | R | R | CRU (ביום האירוע בלבד) | — |
| הגדרות מתחם | CRUD | — | — | — | — |
| ניהול משתמשים / תפקידים | CRUD | — | — | — | — |
| נתונים כספיים / EasyCount | CRUD | R (ללא הפקת מסמכים) | — | — | — |
| משוב / סטטיסטיקות | CRUD | R + שליחת קישור | — | — | Submit only |
| שליחת ברכה | CRUD | R + Send | — | — | — |
| גלריה | CRUD | R | CRU (העלאה) | R | — |

---

# 3. דרישות פונקציונליות (Functional Requirements)

## 3.1 מפת מסכים וניווט

```
התחברות (Google)
    └── לוח בקרה (Dashboard)
            ├── לוח שנה (Calendar) ──► פופאפ יום ──► אופציה / הזמנה / LIVE
            ├── הגדרות מתחם (Settings)
            ├── ניהול אופציות (Options Manager)
            ├── ניהול הזמנות (Bookings Manager)
            ├── משובי לקוחות (Feedback Manager)
            ├── סטטיסטיקות וחישובים (Feedback Stats)
            ├── שליחת ברכה (Greeting Blast)
            ├── טופס הפקת אירוע (Event Form Manager)
            └── גלריה (Gallery)

דף ציבורי: משוב לקוח (/feedback/:token)
```

**תפריט צד (RTL):** מגירה נפתחת מימין, כותרת אחידה בכל מסכי האדמין.

---

## 3.2 מודול 1: התחברות ואבטחה

### FR-AUTH-01: התחברות Google

- כפתור "התחברות עם Google" במסך מרכזי
- בדיקה מול טבלת `AuthorizedUser` — רק אימיילים מורשים
- JWT ב-cookie (Access 1 שעה, Refresh 7 ימים) + CSRF protection

### FR-AUTH-02: ניהול משתמשים מורשים ותפקידים

- הוספה/מחיקה של אימיילי Google בהגדרות מתחם
- הקצאת תפקיד RBAC (`manager` / `staff` / `production` / `floor_staff`) לכל משתמש
- רק **Manager** רשאי לנהל משתמשים ולשנות תפקידים

### FR-AUTH-03: התנתקות

- ניקוי session + redirect למסך התחברות

---

## 3.3 מודול 2: לוח בקרה (Dashboard)

### FR-DASH-01: תצוגת KPI

- מספר הזמנות פעילות, אופציות פתוחות, אירועים קרובים
- ממוצע משוב חודשי (אוכל / שירות / אולם)

### FR-DASH-02: אירועים קרובים

- רשימת 5–10 אירועים הבאים עם סוג, תאריך, מספר אורחים, סטטוס LIVE

### FR-DASH-03: מיני-לוח שנה

- תצוגה חודשית מקוצרת עם ניווט מהיר ללוח המלא

---

## 3.4 מודול 3: לוח שנה (Calendar)

### FR-CAL-01: תצוגת חודש RTL

- עמודות: שבת → ראשון
- תאריך עברי + לועזי בכל תא
- ניווט חודשים, סינון לפי סוג אירוע

### FR-CAL-02: קידוד צבעים למשבצות זמן

| משבצת | צבע |
|-------|-----|
| בוקר | ירוק בהיר |
| צהריים | כחול |
| ערב | סגול |
| לילה | כהה |

### FR-CAL-03: סטטוסים בלוח

- **OPTION** — אופציה (עם תאריך תפוגה)
- **BOOKED** — הזמנה סגורה
- **TODAY** — באנר "אירוע היום" (זיהוי אוטומטי לפי תאריך)
- **LIVE** — אירוע פעיל בקבלה (תג פועם; נפתח ידנית על ידי Manager)
- **BLOCKED** — משבצת חסומה

### FR-CAL-04: פופאפ יום (Day Modal)

- רשימת אירועים ליום הנבחר
- פעולות (לפי RBAC): שמירת אופציה, סגירת הזמנה, **"פתח אירוע LIVE"** (Manager בלבד), טופס קבלה באולם, צפייה בחוזה

### FR-CAL-05: סנכרון בזמן אמת

- עדכון לוח השנה ב-Socket.io בעת שינוי הזמנה/אופציה

---

## 3.5 מודול 4: שמירת אופציה (Option)

### FR-OPT-01: טופס אופציה קצר

- תאריך (ממולא מראש), סוג אירוע, שמות לקוח א/ב, טלפון, הערות
- בחירת משבצת זמן
- משך תפוגה (ברירת מחדל: 48 שעות — מ-`SystemSettings`)

### FR-OPT-02: ניהול אופציות (Options Manager)

- טבלה: תאריך, שמות, סוג, משבצת, תפוגה, פעולות
- חיפוש וסינון
- פעולות (לפי RBAC): הודעה ללקוח (WhatsApp/Email), סגירה להזמנה; **מחיקה — Manager בלבד**

### FR-OPT-03: הודעת אופציה

- מודל עם תבנית הודעה מותאמת
- שליחה בערוץ נבחר: **WhatsApp API רשמי** (Green API / Twilio) או Email
- מעקב סטטוס משלוח (נשלח / נכשל) ורישום בלוג

### FR-OPT-04: מדיניות תפוגה

- אופציה שפגה **נשארת על הלוח** עד סגירת אירוע אמיתי או שחרור ידני (`POST /api/bookings/release`)

---

## 3.6 מודול 5: הזמנה (Booking Form)

### FR-BOOK-01: MetaBar

- קוד אירוע ייחודי (`eventCode`), סטטוס, תאריך

### FR-BOOK-02: פרטי לקוח א' ו-ב'

- שם מלא, ת.ז., טלפון, אימייל, כתובת

### FR-BOOK-03: הגדרות אירוע

- סוג: חתונה / אירוסין / בר מצווה / אחר
- תאריך + תאריך עברי (Hebcal)
- משבצת זמן, מספר אורחים, מינימום אורחים, כשרות, מוזיקה, קוד עוקב

### FR-BOOK-04: תשלומים (רישום ידני)

> **הבהרה:** בשלב זה **אין** אינטגרציה לסליקת אשראי מקוונת. כל התשלומים מנוהלים ונרשמים ידנית במערכת.

- מחיר בסיס לאורח, מע"מ (17–18%), תוספות אולם, תוספות חיצוניות
- אמצעי תשלום נתמכים: **מזומן / העברה בנקאית / צ'ק בלבד**
- **סריקת צ'ק (OCR):** מצלמה + Tesseract לזיהוי מספר צ'ק, בנק, סניף, תאריך
- תבניות תנאי תשלום (`paymentTemplates`)
- רישום סכומים ששולמו (`paidAmount`, `advancePaid`, `totalPaid`) ללא חיוב אוטומטי

### FR-BOOK-04a: אינטגרציית EasyCount (הפקת מסמכים)

**מצב הפעלה:**

| סביבה | ערך `EASYCOUNT_MODE` | תיאור |
|-------|----------------------|--------|
| פיתוח / UAT | `simulation` (ברירת מחדל) | הפקת מסמכים מדומה, ללא קריאה ל-API חי |
| פרודקשן | `live` (מתג `.env`) | הפקה אמיתית מול EasyCount API |

**3 סוגי מסמכים נתמכים:**

| סוג מסמך | EasyCount Doc Type | מתי מופק | מי מפיק |
|----------|-------------------|----------|---------|
| **הצעת מחיר** (Price Quote) | לפני חתימת חוזה | בעת הכנת הצעה ללקוח | Staff / Manager |
| **חשבונית-קבלה** (Invoice-Receipt) | 405 | בעת קבלת מקדמה / תשלום | Manager |
| **חשבונית חיוב** (Debit Invoice) | בסיום LIVE | עבור תוספות LIVE (`EventAddition`) | Manager (אוטומטי בסגירת LIVE) |

- שמירת `easycountDocId`, `easycountDocUrl`, `easycountStatus` ב-Booking
- במצב כשל: סטטוס `FAILED` + המשך תפעול מקומי

### FR-BOOK-05: חוזה

- טקסט חוזה דינמי מ-`SystemSettings.contractText`
- לוח חתימה דיגיטלי (`react-signature-canvas`)
- שמירת חתימה ב-S3

### FR-BOOK-06: שמירה וסגירה

- כפתור "שמור וסגור הזמנה"
- אילוץ ייחודיות: תאריך + משבצת (מניעת double booking)
- יצירת PDF חוזה (Puppeteer)

### FR-BOOK-07: עריכה

- נתיב `/booking/edit/:id` — עריכת הזמנה קיימת
- נתיב `/booking/close-option/:optionId` — המרת אופציה להזמנה

---

## 3.7 מודול 6: ניהול הזמנות (Bookings Manager)

### FR-BM-01: טבלת הזמנות

- עמודות: קוד, תאריך, לקוח, סוג, אורחים, סטטוס תשלום
- חיפוש לפי קוד / שם / טלפון
- עימוד (pagination)

### FR-BM-02: מודל פרטי הזמנה

- צפייה מלאה בפרטים
- כפתורים: עריכה, PDF, מעבר לטופס הפקה

### FR-BM-03: ביטול הזמנה

- רישום ב-`CancellationLog` עם סיבה

---

## 3.8 מודול 7: טופס הפקת אירוע (Event Production Form)

### FR-PROD-01: בחירת הזמנה

- בורר הזמנה פעילה (BOOKED)

### FR-PROD-02: פרטי הפקה

- מספר אורחים סופי, סוג קבלה, חלוקת ישיבה (גברים/נשים %)
- שולחן כבוד, מפות, מרכזי שולחן, כיסא גשר

### FR-PROD-03: תפריט

- בחירת מנות לפי קטגוריות (`MenuCategory` / `Dish`)
- הערות אלרגיות
- תצוגת תפריט (`/menu`)

### FR-PROD-04: כשרות

- בחירת תעודות כשרות (`KashrutCertificate`) — גלובליות + ספציפיות

### FR-PROD-05: בידור וצוות

- מספר מנות לבר, ישיבה, גברים/נשים
- DJ, צלם, מפיק

### FR-PROD-06: תוכנית ישיבה (Floor Plan)

- קנבס עם שולחנות עגולים/מלבניים (גרירה — `react-draggable`)
- שמירת מיקומים ב-`TablePosition`
- ייצוא תמונת תוכנית (`tableLayoutImageUrl`)
- **אופטימיזציה מלאה לטאבלט (iPad):** zoom/pan, גרירה במגע, יעדי מגע מינימליים 44px

### FR-PROD-07: תוספות ומחיר

- חישוב אוטומטי: מחיר מנה, תוספת כשרות, עיצוב, שירותים נוספים
- שדות מחיר מוסתרים (`hiddenPriceFields`)

---

## 3.9 מודול 8: קבלה LIVE (Event Check-In)

### FR-LIVE-01: זיהוי אירוע ביום האירוע (טריגר היברידי)

המערכת מיישמת מודל טריגר **אוטומטי + ידני**:

| שלב | התנהגות | מי מפעיל |
|-----|---------|----------|
| **זיהוי אוטומטי** | ביום תאריך האירוע — תצוגת באנר **"אירוע היום"** בלוח השנה ובפופאפ היום | המערכת (Cron / בדיקת תאריך) |
| **הפעלת LIVE** | מעבר לסטטוס **LIVE** ופתיחת טופס קבלה דיגיטלי | **Manager בלבד** — כפתור **"פתח אירוע LIVE"** |
| **סגירת LIVE** | מעבר למצב "נסגר" + הפקת חשבונית חיוב | Manager |

- תג **LIVE** (פועם) מוצג רק לאחר הפעלה ידנית
- באנר בטופס: "אירוע LIVE — ניתן לעריכה"
- **Floor Staff** רואה את מסך הקבלה רק לאחר שה-Manager פתח את ה-LIVE

### FR-LIVE-02: טופס קבלה

- שדות ממולאים מההזמנה: מנות מוזמנות, משפחות, הערות
- טבלת תוספות LIVE (`EventAddition`): תיאור, עלות, שם עובד, חתימה, אישור תנאים
- חתימת לקוח נדרשת
- **אופטימיזציה מלאה לטאבלט (iPad)** — ממשק מגע, שדות גדולים, חתימה על מסך מגע

### FR-LIVE-02a: מנגנון Fallback (מצב Offline חלקי)

- שמירת נתוני קבלה ב-`localStorage` בדפדפן בעת אובדן קישוריות
- תצוגת אינדיקטור "מצב לא מקוון — הנתונים יסונכרנו"
- סנכרון אוטומטי לשרת (`POST /api/check-in`) עם שחזור קישוריות
- מניעת אובדן נתונים במהלך תקלות אינטרנט מקומיות באולם

### FR-LIVE-03: מצב סגור

- לאחר סיום האירוע: שדות read-only + תג "נסגר"
- עדכון `liveAdditionsTotal` בהזמנה
- הפקה אוטומטית של **חשבונית חיוב** (Debit Invoice) ב-EasyCount עבור תוספות LIVE

---

## 3.10 מודול 9: הגדרות מתחם (Settings)

### טאב תמחור

- מחיר לאורח, מע"מ, עלות כשרות, מקדמה, מחירי בר/צוות/עיצוב
- תבניות תנאי תשלום

### טאב שירותים נוספים

- רשימת `ExtraService` (שם + מחיר + קטגוריה) — CRUD

### טאב כשרות

- ניהול תעודות כשרות + תמונות

### טאב משתמשים מורשים

- רשימת אימיילי Google מורשים
- הקצאת תפקיד RBAC לכל משתמש (Manager / Staff / Production / Floor Staff)
- ניהול — **Manager בלבד**

### טאב צוות (`StaffMember`)

- רשימת שמות עובדים לשימוש בתוספות LIVE

### טאב חוזה

- עריכת טקסט חוזה ברירת מחדל

---

## 3.11 מודול 10: משוב לקוחות (Feedback)

### FR-FB-01: ניהול משוב (אדמין)

- טבלה: תאריך אירוע, לקוח, דירוגים (אוכל/שירות/אולם 1–5), הערות
- כפתור "שלח קישור משוב" — **WhatsApp API רשמי** (Green API / Twilio) או Email
- מעקב סטטוס שליחה (`lastWhatsappSent`, `lastEmailSent`)
- תמיכה בשני צדדים (לקוח א' / ב') — `clientSide`

### FR-FB-04: אוטומציה

- Cron: שליחת קישורי משוב אוטומטית לאחר סיום אירוע
- שליחה דרך **WhatsApp API רשמי** לאמינות ומסירה אוטומטית
- התראה למנהל במייל על משוב חדש

### FR-FB-02: דף משוב ציבורי

- נתיב: `/feedback/:token`
- ללא ניווט אדמין
- 3 קבוצות כוכבים + שדה הערות + שליחה
- Token חד-פעמי, מסומן `isCompleted` לאחר מילוי

### FR-FB-03: סטטיסטיקות (`/feedback-stats`)

- גרפים (Recharts): ממוצעים חודשיים/שנתיים, מגמות
- סינון לפי שנה/חודש

---

## 3.12 מודול 11: שליחת ברכה (Greeting Blast)

### FR-GR-01: בחירת נמענים

- רשימת checkbox מהזמנות פעילות

### FR-GR-02: עורך הודעה

- נושא, גוף הודעה, צירוף קובץ

### FR-GR-03: שליחה

- מיידית או מתוזמנת (`ScheduledGreeting`)
- Cron לעיבוד ברכות שתוזמנו (`processDueScheduledGreetings`)
- סטטוס: PENDING / SENT / FAILED + `sendStats`

---

## 3.13 מודול 12: גלריה (Gallery)

### FR-GAL-01: מטרת הגלריה

הגלריה משמשת כ**מאגר ויזואלי תפעולי** לצוות ההפקה — תמונות מאירועים שבוצעו (לוגיסטיקה, סידורי שולחנות, עיצובים) לשימוש כ**התייחסות ויזואלית** בעת ייעוץ ללקוחות עתידיים.

### FR-GAL-02: סוגי תוכן

- תמונות סידורי שולחנות ותוכניות ישיבה
- תמונות עיצוב אולם (פרחים, תאורה, מרכזי שולחן)
- תמונות לוגיסטיקה (הקמה, מטבח, קבלה)

### FR-GAL-03: הרשאות והעלאה

| פעולה | Manager | Production | Staff | Floor Staff |
|-------|---------|------------|-------|-------------|
| העלאת תמונות | ✓ | ✓ | — | — |
| צפייה | ✓ | ✓ | ✓ | — |
| מחיקה | ✓ | — | — | — |

- אחסון ב-S3 עם הצפנה (SSE)
- תיוג לפי סוג אירוע / תאריך (אופציונלי)

---

## 3.14 מודול 13: נגישות (Accessibility)

### FR-ACC-01

- וידג'ט נגישות צף (`AccessibilityWidget`)
- הגדרות: גודל טקסט, ניגודיות, הדגשת קישורים
- שמירה ב-`AccessibilityContext` + localStorage

---

## 3.15 זרימות משתמש מרכזיות (User Flows)

### זרימה 1: אופציה → הזמנה

```
לוח שנה → בחירת תאריך → פופאפ יום → "שמירת אופציה"
→ מילוי טופס קצר → שמירה (סטטוס OPTION)
→ [תפוגה / המשך] → "סגור להזמנה" → טופס הזמנה מלא
→ תשלום + חתימה → שמירה (סטטוס BOOKED)
```

### זרימה 2: הזמנה → הפקה → LIVE

```
ניהול הזמנות → בחירת הזמנה → טופס הפקה
→ תפריט + תוכנית ישיבה → שמירה
→ [יום האירוע] → באנר אוטומטי "אירוע היום"
→ Manager לוחץ "פתח אירוע LIVE"
→ Floor Staff / Manager: טופס קבלה (עם Fallback Offline)
→ תוספות LIVE + חתימה → סגירה → חשבונית חיוב (EasyCount)
```

### זרימה 3: אירוע → משוב

```
[Cron: סיום אירוע] → יצירת Feedback + Token
→ שליחת קישור ללקוח → מילוי דף ציבורי
→ עדכון סטטיסטיקות + התראה למנהל
```

---

# 4. דרישות לא-פונקציונליות (Non-Functional Requirements)

## 4.1 ביצועים (Performance)

| מדד | יעד |
|-----|-----|
| טעינת לוח שנה (חודש) | < 2 שניות |
| שמירת הזמנה | < 3 שניות |
| עדכון real-time | < 500ms latency |
| יצירת PDF | < 10 שניות |
| עומס מקבילי | עד 10 משתמשים בו-זמנית (אולם בינוני) |

**המלצות:**

- Lazy loading לרכיבי React (`React.lazy`)
- TanStack Query עם cache invalidation מבוקר
- אינדקסים ב-DB על `EventDate.date`, `Booking.eventCode`, `Feedback.token`

## 4.2 אבטחה (Security)

| דרישה | מימוש |
|-------|-------|
| אימות | Google OAuth + JWT ב-HttpOnly cookies |
| הרשאות | RBAC — 4 תפקידים + רשימת `AuthorizedUser` |
| CSRF | middleware `csrfProtection` |
| Rate limiting | 500 req/15min API, 150 write, 20 login |
| Headers | Helmet.js |
| CORS | רשימת origins מורשים בלבד |
| קלט | Validation עם Zod בכל endpoint |
| קבצים | Multer + S3 (SSE), בדיקת סוג קובץ |
| הצפנת אחסון | כל קבצים רגישים (חוזים, צ'קים, חתימות) ב-S3 עם **Server-Side Encryption (SSE-S3 / SSE-KMS)** |
| גישה לקבצים | **Presigned URLs** מוגבלות בזמן, מופקות ע"י השרת למשתמשים מאומתים ומורשים בלבד — ללא גישה ישירה ל-bucket |
| פרטיות נתונים | עמידה בעקרונות חוק הגנת הפרטיות הישראלי; מינימום גישה ל-PII לפי RBAC |
| לוגים | Winston + daily rotate, ללא PII רגיש |
| ניטור | Sentry (client + server) |

## 4.3 זמינות, אמינות ופריסה (Availability & Hosting)

### תשתית ענן (AWS)

| רכיב | שירות AWS | תיאור |
|------|-----------|--------|
| אפליקציה (Server + Client) | **ECS** או **App Runner** | פריסת Node.js + הגשת React build |
| מסד נתונים | **RDS PostgreSQL** (Managed) | Multi-AZ מומלץ לפרודקשן |
| אחסון קבצים | **S3** | חוזים, צ'קים, חתימות, תמונות גלריה |
| גיבויים | RDS Automated Backups + S3 versioning | RPO < 24 שעות |

| דרישה | יעד |
|-------|-----|
| Uptime | 99.5% (שעות עסקים) |
| גיבוי DB | RDS Automated Backup + Cron יומי (03:00) — `BACKUP_ENABLED=true` |
| שחזור | RPO < 24 שעות, RTO < 4 שעות |
| Graceful shutdown | ניתוק Socket.io נקי |

### המשכיות תפעולית (Operational Continuity / SLA)

| תרחיש | מנגנון מיטיגציה |
|-------|-----------------|
| תקלת אינטרנט מקומית באולם | **Fallback Offline** — שמירת נתוני קבלה LIVE ב-`localStorage`, סנכרון אוטומטי עם שחזור קישוריות |
| תקלת שרת AWS | RDS failover אוטומטי; ECS/App Runner health checks + restart |
| תקלת EasyCount | המשך תפעול מקומי + סטטוס `FAILED` + הפקה ידנית מאוחרת |
| תקלת WhatsApp API | retry + fallback לאימייל; רישום בלוג |

## 4.4 תאימות מכשירים ודפדפנים

| פלטפורמה | תמיכה |
|----------|--------|
| Desktop | עדיפות ראשונה (Chrome, Edge, Firefox) — מסכי אדמין, לוח שנה, הזמנות |
| **Tablet (iPad)** | **אופטימיזציה מלאה** למסכי **קבלה LIVE** ו**תוכנית ישיבה** — ממשק מגע, גרירת שולחנות, חתימה דיגיטלית |
| Mobile | תצוגה responsive לשאר המסכים; לא Native |
| RTL | מלא — כל הממשק בעברית |
| נגישות | WCAG 2.1 רמה AA (יעד) |

**דרישות אופטימיזציה לטאבלט (iPad):**

- מסך קבלה LIVE: שדות קלט גדולים (min 44px touch target), מקלדת מספרית לסכומים
- תוכנית ישיבה: גרירה חלקה של שולחנות, zoom/pan (`react-zoom-pan-pinch`)
- תמיכה ב-Safari iOS (דפדפן ברירת מחדל ב-iPad)
- בדיקות QA ייעודיות על iPad לפני כל release

## 4.5 אינטגרציות חיצוניות

| שירות | שימוש | מצב כשל |
|-------|-------|---------|
| Google OAuth | התחברות | חסימת כניסה |
| **EasyCount** | 3 סוגי מסמכים: הצעת מחיר, חשבונית-קבלה, חשבונית חיוב. ברירת מחדל: `simulation`; פרודקשן: `live` (`.env`) | שמירה מקומית + סטטוס `FAILED` + המשך תפעול |
| AWS S3 (SSE) | חתימות, צ'קים, תמונות גלריה — Presigned URLs בלבד | retry + הודעת שגיאה |
| AWS RDS | PostgreSQL מנוהל | Failover אוטומטי |
| Nodemailer | אימיילים, תזכורות, ברכות | לוג + המשך תפעול |
| **WhatsApp API** (Green API / Twilio) | תזכורות אופציה, קישורי משוב, התראות — **מסירה אוטומטית אמינה** | retry (3 ניסיונות) + fallback לאימייל + רישום בלוג |
| Hebcal | תאריכים עבריים | fallback לתאריך לועזי |

## 4.6 תחזוקה ותפעול

- משתני סביבה מאומתים ב-`validateEnv()` בהפעלה
- לוגים מובנים (Winston) + Sentry לאירועי שגיאה
- בדיקות E2E (Playwright) — Golden Path להזמנה
- סקריפטים: `verify-no-double-booking`, `migrate-legacy-event-codes`, **`db:import-legacy`**
- פריסה: AWS ECS / App Runner + RDS PostgreSQL

---

# 5. מודל נתונים וארכיטקטורה (Data Model & Architecture)

## 5.1 דיאגרמת ישויות (ER — תמציתי)

```
EventDate (1) ──────< Booking (N)
                         │
         ┌───────────────┼───────────────┬──────────────┐
         │               │               │              │
    EventForm (1)   EventCheckIn (1)  Feedback (N)  EventAddition (N)
         │
    TablePosition (N)

MenuCategory (1) ──< Dish (N)
SystemSettings (1) — singleton
ExtraService (N)
KashrutCertificate (N)
AuthorizedUser (N) — כולל שדה role (RBAC)
StaffMember (N)
ScheduledGreeting (N)
CancellationLog (N)
```

## 5.2 ישויות מרכזיות

### Booking (הזמנה / אופציה)

- ישות מרכזית — מכילה פרטי לקוח, תשלום, חוזה, קוד אירוע
- `isOption: true` = אופציה, `false` = הזמנה סגורה
- אילוץ: `@@unique([calendarDateId, timeSlot])` — מניעת כפילות

### EventDate (תאריך בלוח)

- תאריך ייחודי, סטטוס (OPTION / BOOKED / BLOCKED)
- `optionExpiresAt` לתפוגת אופציה

### EventForm (הפקה)

- פרטי הפקה, תפריט (JSON), תוכנית שולחנות
- קשור 1:1 ל-Booking

### EventCheckIn (קבלה LIVE)

- נתוני קבלה בפועל, חתימת לקוח
- קשור 1:1 ל-Booking

### Feedback

- Token ייחודי, דירוגים, `clientSide` (A/B)
- אילוץ: `@@unique([bookingId, clientSide])`

### SystemSettings

- singleton (`id: "global"`) — כל הגדרות התמחור והחוזה

### AuthorizedUser

- רשימת אימיילי Google מורשים + שדה `role` (RBAC: `manager` / `staff` / `production` / `floor_staff`)
- ניהול והקצאת תפקידים — Manager בלבד

## 5.3 ארכיטקטורת מערכת (AWS)

```
┌─────────────────────────────────────────────────────────┐
│              Client (React + Vite — CDN / S3)            │
│  Router │ TanStack Query │ Socket.io │ LocalStorage     │
│  (Fallback Offline — LIVE Check-In)                      │
└────────────────────────┬────────────────────────────────┘
                         │ HTTPS / WSS
┌────────────────────────▼────────────────────────────────┐
│           AWS ECS / App Runner (Node.js + Express)         │
│  Routes → Controllers → Services → Prisma ORM           │
│  Middlewares: Auth, RBAC, CSRF, RateLimit, Helmet        │
│  Cron Jobs │ Socket.io │ Puppeteer (PDF) │ Presigned URL│
└─────┬──────────┬──────────┬──────────┬──────────────────┘
      │          │          │          │
  AWS RDS     AWS S3     EasyCount   WhatsApp API
 (PostgreSQL)  (SSE)        API     (Green API / Twilio)
                              │
                          Nodemailer
```

## 5.4 מחסנית טכנולוגית (Tech Stack)

| שכבה | טכנולוגיה |
|------|-----------|
| Frontend | React 19, TypeScript, Vite 8, React Router 7 |
| State | TanStack Query 5, Zustand |
| UI | CSS Modules, רכיבי UI פנימיים |
| Backend | Node.js, Express 5, TypeScript |
| ORM | Prisma 6 |
| DB | PostgreSQL (AWS RDS — Managed) |
| Hosting | AWS ECS / App Runner |
| Auth | Google OAuth, JWT, RBAC (4 roles) |
| Real-time | Socket.io 4 |
| Storage | AWS S3 (SSE + Presigned URLs) |
| PDF | Puppeteer |
| OCR | Tesseract.js + react-webcam |
| Charts | Recharts |
| Email | Nodemailer |
| Monitoring | Sentry |
| Testing | Playwright |
| Scheduling | node-cron |

## 5.5 API — קבוצות Endpoints עיקריות

| קבוצה | נתיב בסיס | תיאור |
|-------|-----------|--------|
| Auth | `/api/auth` | login, refresh, authorized-users |
| Calendar | `/api/calendar` | תאריכים, חסימות |
| Bookings | `/api/bookings` | CRUD, אופציות, תוספות LIVE |
| Event Form | `/api/event-form` | הפקה, שולחנות |
| Settings | `/api/settings` | תמחור, שירותים, צוות |
| Feedback | `/api/feedback` | admin + public submit |
| Check-In | `/api/check-in` | קבלה LIVE |
| Menu | `/api/menu` | קטגוריות ומנות |
| Kashrut | `/api/kashrut` | תעודות |

---

# 6. הנחיות UI/UX (UI/UX Guidelines)

## 6.1 עקרונות עיצוב

- **סגנון:** כלי אדמין מקצועי, רגוע, עסקי-פנימי
- **כיוון:** RTL מלא — כל הטקסטים, טבלאות, מגירות מימין
- **שפה:** עברית בלבד בממשק האדמין
- **Desktop-first:** responsive לטאבלט; **אופטימיזציה מלאה ל-iPad** במסכי LIVE ותוכנית ישיבה

## 6.2 מערכת עיצוב (Design System)

| אלמנט | ערך |
|--------|-----|
| Primary Green | `#8CBF8E` |
| Green Dark | `#6BA86E` |
| Green Deep | `#4F8F52` |
| Background | `#F7F8F6` |
| Surface/Cards | `#FFFFFF` |
| Text Primary | `#2D3748` |
| Text Secondary | `#4A5568` |
| Text Muted | `#718096` |
| Danger | `#E53E3E` |
| Warning | `#DD6B20` |
| Info | `#4A6FA5` |
| Font | Segoe UI |
| Border Radius | 10–14px |
| Shadows | רכות, עדינות |

## 6.3 רכיבי UI סטנדרטיים

- **כותרת אחידה** (`AppHeader`) — המבורגר, לוגו מיפל (עלה + "מיפל"), כותרת עמוד
- **מגירת ניווט** (`AppSidebar`) — slide מימין
- **כרטיסים** (`Card`) — קיבוץ תוכן בטפסים
- **טבלאות** (`DataTable`) — עם חיפוש, עימוד, מיון
- **מודלים** — פופאפי יום, פרטי הזמנה, חוזה
- **Badges** — סטטוס "אירוע היום", LIVE (פועם), OPTION, BOOKED
- **Skeleton** — טעינה מדורגת
- **EmptyState** — מצבים ריקים עם הנחיה

## 6.4 אנימציות ואינטראקציות

| אלמנט | אנימציה |
|-------|---------|
| תג LIVE | Pulse / glow עדין |
| מגירת ניווט | Slide-in מימין, 300ms ease |
| מודלים | Fade overlay + scale-in |
| כפתורי CTA | Hover: darken + shadow |
| טעינה | Skeleton shimmer |
| Toast הצלחה/שגיאה | Slide-down מלמעלה |

## 6.5 הנחיות טפסים

- חלוקה לסקשנים בכרטיסים (`MetaBar`, `EventSettings`, `Payment`, `Contract`)
- שדות חובה מסומנים ב-`*`
- ולידציה inline + הודעות שגיאה בעברית
- לוח חתימה: אזור גדול, כפתור "נקה" ו-"אישור"

## 6.6 לוח שנה — UX

- תא "היום" מודגש
- צבעי משבצות ברורים עם מקרא (Legend)
- Hover על תא: tooltip עם סיכום אירועים
- לחיצה על תא תפוס: פופאפ (לא ניווט ישיר)
- תאריך עברי בגופן קטן יותר מתחת ללועזי

## 6.7 דף משוב ציבורי

- מינימליסטי, ללא ניווט אדמין
- לוגו מרכזי + "נשמח לשמוע מכם"
- כוכבים גדולים ונגישים (tap-friendly)
- CTA ירוק בולט: "שליחה"
- מסך תודה לאחר שליחה

## 6.8 נגישות

- וידג'ט נגישות צף בכל העמודים
- `role`, `aria-label` ברכיבים אינטראקטיביים
- ניגודיות מינימלית 4.5:1
- ניווט מקלדת בטפסים ומודלים

---

# 7. נספחים

## 7.1 נתוני דוגמה (Seed Data)

| אירוע | פרטים |
|-------|--------|
| חתונה כהן-לוי | 15.08.2026, ערב, 350 אורחים, **LIVE** |
| אופציה — אירוסין שמש-ברק | 22.09.2026, צהריים |
| בר מצווה משפחת אברהם | 03.07.2026, בוקר |
| טלפון אולם | 03-6777772 |
| מחיר בסיס | ₪280 לאורח |

## 7.2 Cron Jobs מתוכננים

| תזמון | משימה |
|-------|--------|
| 09:00 יומי | תזכורות צ'ק ביטחון, טופס הפקה, בחירת תפריט |
| לאחר אירוע | יצירת משוב + שליחת קישור |
| 03:00 יומי | גיבוי מסד נתונים |
| לפי תזמון | שליחת ברכות מתוזמנות |

## 7.3 סיכונים ומיטיגציה

| סיכון | מיטיגציה |
|-------|----------|
| Double booking | אילוץ DB + בדיקת שרת + מיגרציית Excel לפני go-live |
| אובדן חתימה/צ'ק | S3 (SSE) + גיבוי RDS + Presigned URLs |
| כשל EasyCount | סטטוס `FAILED` + מצב `simulation` ב-UAT + המשך תפעול |
| גישה לא מורשית | RBAC + רשימת אימיילים + JWT |
| תקלת אינטרנט באולם | Fallback Offline (localStorage) במסך LIVE |
| כשל WhatsApp API | retry + fallback לאימייל |

## 7.4 מיגרציית נתונים היסטוריים (Data Migration)

### DR-MIG-01: ייבוא אירועים מ-Excel

**מטרה:** ייבוא הזמנות קיימות לשנים 2026–2027 מקובץ Excel מרכזי, כדי למנוע double-booking של תאריכים שכבר תפוסים במערכת הישנה.

**פקודת הרצה:**

```bash
npm run db:import-legacy -- --file=./data/legacy-bookings-2026-2027.xlsx
```

**דרישות הסקריפט:**

| דרישה | תיאור |
|-------|--------|
| קלט | קובץ Excel מאסטר (`legacy-bookings-2026-2027.xlsx`) עם עמודות: תאריך, משבצת, סוג אירוע, שמות לקוח, טלפון, מספר אורחים, קוד אירוע (אופציונלי) |
| ולידציה | בדיקת כפילויות לפני ייבוא; דילוג על שורות שגויות עם דוח שגיאות |
| מיפוי | יצירת `EventDate` + `Booking` (סטטוס `BOOKED`, `isOption: false`) |
| אילוץ | שמירה על `@@unique([calendarDateId, timeSlot])` — עצירה והתראה במקרה של התנגשות |
| דוח | סיכום: יובאו / דולגו / נכשלו; קובץ לוג `import-legacy-report.json` |
| הרצה | חד-פעמית לפני go-live; ניתן להרצה חוזרת במצב idempotent (דילוג על קיימים) |

**תלות:** יש להשלים את המיגרציה **לפני** הפעלת המערכת בפרודקשן, כדי שהלוח שנה ישקף את כל האירועים התפוסים.

## 7.5 יומן החלטות (Decision Log)

| # | נושא | החלטה | תאריך |
|---|------|--------|--------|
| 1 | הרשאות | RBAC — 4 תפקידים (Manager, Staff, Production, Floor Staff) | 08.07.2026 |
| 2 | EasyCount | `simulation` בפיתוח/UAT; `live` בפרודקשן; 3 סוגי מסמכים | 08.07.2026 |
| 3 | WhatsApp | API רשמי (Green API / Twilio) | 08.07.2026 |
| 4 | גלריה | תמונות הפקה כהתייחסות ויזואלית; העלאה: Manager + Production | 08.07.2026 |
| 5 | LIVE Trigger | באנר אוטומטי "אירוע היום"; הפעלת LIVE ידנית ע"י Manager | 08.07.2026 |
| 6 | תשלומים | ללא סליקת אשראי; רישום ידני (מזומן/העברה/צ'ק + OCR) | 08.07.2026 |
| 7 | Hosting | AWS ECS/App Runner + RDS PostgreSQL; S3 SSE + Presigned URLs | 08.07.2026 |
| 8 | SLA | Fallback Offline (localStorage) במסך LIVE | 08.07.2026 |
| 9 | מכשירים | אופטימיזציה מלאה ל-iPad — LIVE + תוכנית ישיבה | 08.07.2026 |
| 10 | מיגרציה | `npm run db:import-legacy` — Excel 2026–2027 | 08.07.2026 |

---

*מסמך זה נכתב על בסיס הקוד הקיים בפרויקט `Maple-Garden-Events` ומפרט העיצוב ב-`STITCH-SPEC.md`. גרסה 1.1 — מעודכן עם החלטות תפעוליות.*
